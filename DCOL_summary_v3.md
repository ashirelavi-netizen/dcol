# סיכום פרויקט DCOL ל-ZCAD — גרסה 3

מסמך זה מסכם את כל הדרישות, ההחלטות, מצב הקוד, והמשימות הפתוחות. נועד להעברה לצ'אט חדש או למחשב אחר כדי להמשיך ללא אובדן הקשר.

---

## מטרת התוסף
תוסף AutoLISP ל-ZCAD שמצייר צורה סגורה עם האצ', ובו-זמנית יוצר עותק זהה מוזח בשכבות אחרות. בעצם "העתק + שינוי שכבות" אוטומטי, עם סנכרון מאוחר (DCOLUP).

המשתמש אינו מתכנת. עובד עם שרטוטי קונסטרוקציה (עמודים — Columns). הערות הקוד בעברית. כל התשובות בעברית.

---

## הפקודות
| פקודה | תפקיד | סטטוס |
|--------|--------|--------|
| `DCOL` | ציור צורה כפולה (מקור + עותק) | עובד |
| `DCOLUP` | סנכרון כל הזוגות | עובד |
| `DCOLSET` | פתיחת חלון ההגדרות ידנית | עובד |

שם הפקודה: DCOL = Double Column (נבחר כי COL התנגש עם פקודה מובנית).

---

## מצב הקוד הנוכחי

**קבצים:**
- `DCOL.lsp` — כל קוד התוסף. נמצא ב:
  `C:\Users\Owner\Desktop\claude\Acad\files\DCOL.lsp`
- `acaddoc.lsp` — טוען את DCOL.lsp אוטומטית. הועתק לתיקיית Support של ZCAD.
  טעינה אוטומטית מוגדרת דרך Startup Suite ב-APPLOAD — מאושר ועובד.
- `dcol_icon.bmp` / `dcol_icon.png` — אייקון לכפתור בסרגל הכלים (32×32).

גרסת ZCAD (תואם AutoCAD). שם משתמש Windows: Owner.

---

## מה שהושלם בגרסה זו (v3)

### סעיף 2 — גרסת XData
כל אובייקט מתויג מקבל שדה גרסה ("1") כשדה ראשון ב-XData.
מבנה: `(1000 . "1") (1000 . pid) (1000 . role)`

### סעיף 3 — מצב Multi
מצב חדש בתפריט: `[Multi]`
זרימה: גבול חיצוני ← חלוקות פנימיות (כמה שרוצים) ← האצ' טבעתי.
האצ' נבנה מבחירת גבולות בלבד (לא pick-point) — פותר בעיית XREF ברקע.
Roles חדשים: `SOL/SIL` למקור, `COL/CIL` לעותק.

### סעיף 4 — מצב Attach
מצב חדש בתפריט: `[Attach]`
זרימה: בחר האצ' קיים ← בחר קווי מתאר ← התוסף מתייג ויוצר עותק.
תמיכה בהאצ' אסוציאטיבי (בונה מחדש) ולא-אסוציאטיבי (מעתיק כפי שהוא).
Attach לא נשמר כצורה פעילה — מבצע ויוצא.

### סעיף 5 — כפתור "שכבה חדשה"
לחיצה על הכפתור: סוגר הגדרות ← פותח מנהל שכבות (CLASSICLAYER / LAYER) ←
המשתמש לוחץ Enter בשורת הפקודה ← ההגדרות נפתחות מחדש עם רשימה מעודכנת.

### סעיף 6 — כפתור בסרגל כלים
הוראות למשתמש: פתח CUI ← Toolbars ← New Toolbar "DCOL" ← New Button.
מאקרו: `^C^Cdcol`
אייקון: `dcol_icon.bmp` (32×32, שני ריבועים כחול/אדום עם האצ').
**טרם בוצע על ידי המשתמש.**

### סעיף 7 — שיפור pair-id
המזהה משלב שעון + מונה עולה:
`"<millisecs>-<counter>"` למשל `"1748123456-3"`
מונה מאופס בכל טעינת קובץ (משתנה גלובלי `*COL-COUNTER*`).

### סעיף 1 — DCOLUP מלא
לוגיקה מלאה:
- מסרוק כל האובייקטים עם XData של `COL_PAIR`
- קיבוץ לפי pair-id
- לכל זוג: מוחק עותק ישן, בונה עותק חדש מהמקור + הזחה מההגדרות
- אם עותק נמחק — נוצר מחדש
- כפילויות מ-COPY של המשתמש — מדולגות עם הודעה
- תומך: רגיל (P/C/E), Multi, Attach (אסוציאטיבי + לא-אסוציאטיבי)

---

## מבנה ה-XData (זיהוי זוגות)
- שם אפליקציה: `COL_PAIR` (קבוע — אסור לשנות)
- שלושה קודי 1000: גרסה ("1") + pair-id + role
- Roles רגיל: "SL" / "SH" / "CL" / "CH"
- Roles Multi: "SOL" / "SIL" / "SH" / "COL" / "CIL" / "CH"
- **עיקרון מחייב:** זיהוי אך ורק דרך XData — לעולם לא לפי שכבה/צבע.

---

## מבנה ההגדרות (נשמר פר-קובץ ב-DWG)
מילון `COL_SETTINGS`, XRecord עם 14 שדות (0–13):
- 0: שכבת קו מקור | 1: שכבת האצ' מקור
- 2: שכבת קו עותק | 3: שכבת האצ' עותק
- 4: Pattern מקור | 5: Scale מקור | 6: Angle מקור
- 7: Pattern עותק | 8: Scale עותק | 9: Angle עותק
- 10: dX | 11: dY | 12: dZ
- 13: צורה פעילה ("P"/"C"/"E"/"M") — "A" לא נשמר

---

## פרטים טכניים
- ציור: `getpoint/getdist` (תומך osnap), לא grread
- האצ': `-HATCH` עם בחירת גבולות; שכבה נכפית עם entmod
- pair-id: millisecs + מונה גלובלי
- לא-אסוציאטיבי: מועתק עם `command "_.COPY"` + entmod לשכבה
- סביבה: ZCAD, Windows, שם משתמש Owner

---

## משימות פתוחות
1. **הוספת כפתור בסרגל הכלים** — המשתמש טרם ביצע, ראה הוראות בסעיף 6 למעלה.
2. **בדיקת DCOLUP בפועל** — הלוגיקה נכתבה אך טרם נבדקה על שרטוט אמיתי.
3. **בדיקת מצב Multi ו-Attach** — נכתבו, טרם נבדקו.
4. **שיפור טיפול בכפילויות COPY** — כרגע מדולג; בעתיד אפשר לתת אפשרות לפצל לזוגות נפרדים.
