# סיכום פרויקט DCOL — גרסה 9

מסמך זה מסכם את כל הדרישות, ההחלטות, מצב הקוד, והמשימות הפתוחות.
נועד להעברה לצ'אט חדש.

**כלל ניהול גרסאות:** כשנוצר סיכום חדש — מוחקים את הישן מיד, גם מקומית וגם מ-GitHub. תמיד מחזיקים רק גרסה אחת עדכנית.

---

## כללי פורמט בצ'אט
- כל התשובות בעברית.
- המשתמש אינו מתכנת. להסביר בשפה פשוטה.
- עובד עם שרטוטי קונסטרוקציה (עמודים) ב-ZCAD.
- **כשיש מילה אנגלית בתוך משפט עברי — עוברים לשורה חדשה לפניה ואחריה.**
  לדוגמה: במקום "הפונקציה col:mk-hatch יוצרת" — כותבים:
  "הפונקציה
  col:mk-hatch
  יוצרת"
  **חובה ליישם כלל זה בכל תשובה, בכל סיכום, ללא יוצא מן הכלל.**
- **לפני כל תיקון — לבקש אישור מהמשתמש.**

---

## מטרת התוסף
תוסף
AutoLISP
ל-ZCAD שמצייר צורה סגורה עם האצ', ובו-זמנית יוצר עותק זהה מוזח בשכבות אחרות. עם סנכרון מאוחר (DCOLUP).

---

## קבצים
| קובץ | מיקום | תפקיד |
|------|--------|--------|
| `DCOL.lsp` | `C:\Users\Owner\Desktop\claude\Acad\dcol\` | כל קוד התוסף |
| `acaddoc.lsp` | `C:\Users\Owner\Desktop\claude\Acad\dcol\` | טוען DCOL.lsp אוטומטית |
| `dcol_icon.bmp` | `C:\Users\Owner\Desktop\claude\Acad\dcol\` | אייקון לכפתור (32×32) |
| `DCOL_summary_v9.md` | `C:\Users\Owner\Desktop\claude\Acad\dcol\` | סיכום הפרויקט (זה הקובץ) |
| `TEST_TOOLBAR.lsp` | `C:\Users\Owner\Desktop\claude\Acad\dcol\` | קובץ בדיקת תמיכה בסרגל כלים (לא קשור לזרימת DCOL) |

מאגר קוד ב-GitHub:
`https://github.com/ashirelavi-netizen/dcol`

טעינה אוטומטית: מוגדרת דרך
Startup Suite
ב-APPLOAD — עובד.
רענון ידני (בלי לסגור):
APPLOAD ← Load
את
DCOL.lsp.

**פרויקט נפרד, לא חלק מ-DCOL:** סרגל כלים משותף לכמה תוספים — מתועד ב-
`C:\Users\Owner\Desktop\claude\Acad\bar\BAR_summary_v1.md`.
לא לטפל בו בצ'אט הזה.

---

## הפקודות
| פקודה | תפקיד | סטטוס |
|--------|--------|--------|
| `DCOL` | ציור צורה כפולה + סנכרון | עובד |
| `DCOLUP` | סנכרון כל הזוגות (פקודה עצמאית) | עובד |
| `DCOLSET` | פתיחת הגדרות ידנית | עובד — **בעיצוב מחדש כרגע, ראה משימות פתוחות** |

---

## שינוי גדול בגרסה זו: הגדרות נשמרות בקובץ, לא בשרטוט

**לפני:** ההגדרות נשמרו בתוך כל קובץ DWG בנפרד (Named Object Dictionary).
**אחרי:** ההגדרות נשמרות בקובץ אחד על המחשב:
`%USERPROFILE%\dcol_settings.dat`

**משמעות:** ההגדרות מוגדרות פעם אחת ועובדות בכל השרטוטים. אם שכבה שנשמרה לא קיימת בשרטוט מסוים — המשתמש יבחר שכבה אחרת ב-DCOLSET עבור אותו שרטוט.

**מנגנון (לפי `settings_pattern.md` המשותף):**
- `col:get-settings` — קורא מהקובץ; `nil` אם אין קובץ
- `col:put-settings` — כותב לקובץ
- `col:ensure-settings` — אם יש קובץ אך חסרים שדות (עדכון LSP) → ממזג בשקט עם ברירות מחדל ושומר; אם אין קובץ כלל → פותח חלון הגדרות (פעם ראשונה בלבד)

קובץ הדפוס המשותף לכל התוספים:
`C:\Users\Owner\Desktop\claude\Acad\settings_pattern.md`

---

## תפריט הפקודה DCOL — דו-שלבי, **כולו בחלונות DCL**

כל התפריטים (עד להקלקה הראשונה בשרטוט) עברו מ-
`getkword`
בשורת הפקודה לחלונות
DCL
עם כפתורים. הקלקת הנקודות בפועל (פינות, קודקודים, מרכז/רדיוס, זרימת אליפסה) **נשארה** בשורה ליד הסמן — לא השתנתה.

הפונקציה הגנרית שבונה את כל התפריטים:
`col:menu-dlg ( title opts default )`
— מקבלת כותרת, רשימת זוגות (key . label), וברירת מחדל; מחזירה את ה-key שנבחר או nil אם בוטל (כפתור "ביטול" גלוי בכל חלון).

### תפריט עליון (DCL):
`ColWall` / `Geometry` / `Settings`

- **Settings** — פותח חלון הגדרות ויוצא
- **ColWall** — עובר לתפריט COL/WALL
- **Geometry** — עובר לתפריט Geometry
- ביטול בחלון → מבטל את הפקודה

### תפריט COL/WALL (DCL):
`Polygon` / `Rectangle` / `Circle` / `Ellipse` / `Multi` / `Attach` / `Update`
ברירת מחדל = הצורה הפעילה האחרונה.

### תפריט Geometry (DCL):
`Border` / `Hole`

**Border** — קו מתאר בלבד + עותק מוזח. **לעולם בלי חור/האצ'.**
תפריט (DCL): `Rectangle` / `Polygon` / `Polyline`
- Rectangle = מלבן סגור
- Polygon = מצולע סגור **(נוסף בגרסה זו)**
- Polyline = פולי-ליין פתוח (2+ נקודות)

**Hole** — צורה סגורה + חישוב פנימי אוטומטי (0.15) + האצ' + עותק מוזח. **תמיד עם חור — אין יותר שאלת Inner/NoInner (הוסרה בגרסה זו).**
תפריט (DCL): `Rectangle` / `Polygon`

**הסיבה להסרת NoInner:** לכל מי שרוצה צורה סגורה בלי חור — Border כבר מכיל את האפשרות (אחרי הוספת Polygon אליו). NoInner היה מיותר.

---

## תיקון Multi — חזרה לשלב הקודם בלי לבטל הכל

**הבעיה שתוקנה:** קודם, Escape בתוך השאלות/בחירות של Multi הפיל את כל הפקודה עם הודעת שגיאה (כי Escape בתוך getkword גורם לביטול אוטומטי שלא ניתן לתפוס בקלות).

**הפתרון:** כל הבחירות בתוך Multi עברו לחלונות DCL:
- `col:ask-more-dlg` — "הוסף חלוקה נוספת?" עם 3 כפתורים גלויים: **כן** / **לא** / **חזרה לשלב הקודם** (לא מסתמך על מקש Escape בפועל — התברר שב-ZCAD הזה Escape לא מפעיל cancel גם כשיש כפתור Cancel גלוי).
- `col:shape-dlg` — בחירת סוג צורה (Polygon/Rectangle/Circle/Ellipse), כפתור Cancel גלוי שעובד בלחיצה.

**לוגיקה חדשה — הצורה החיצונית לעולם לא נמחקת בטעות:**
- `col:multi-ensure-first-inner` — מבטיחה חלוקה פנימית; מבטלת גם את החיצונית רק אם המשתמש מבטל ספציפית כשמתבקש לצייר חלוקה ראשונה.
- `col:multi-undo-last` — מוחקת חלוקה אחרונה; אם זו הייתה היחידה, חוזרת לבקש חלוקה ראשונה מחדש — **הצורה החיצונית נשארת על השרטוט**.

**ממצא חשוב לזכור:** ב-ZCAD הזה, מקש Escape בפועל לא מפעיל כפתורי Cancel/cancel_btn בחלונות DCL בצורה אמינה. לכן בכל חלון שדורש "חזרה"/"ביטול" — חובה כפתור גלוי, לא להסתמך על Escape בלבד.

---

## מבנה ההגדרות (נשמר ב-`%USERPROFILE%\dcol_settings.dat`, שורה לכל שדה)
| אינדקס | תוכן |
|--------|------|
| 0 | שכבת קו מקור |
| 1 | שכבת האצ' מקור |
| 2 | שכבת קו עותק |
| 3 | שכבת האצ' עותק |
| 4 | Pattern מקור |
| 5 | Scale מקור |
| 6 | Angle מקור |
| 7 | Pattern עותק |
| 8 | Scale עותק |
| 9 | Angle עותק |
| 10 | dX |
| 11 | dY |
| 12 | dZ |
| 13 | צורה פעילה: "P"/"R"/"C"/"E"/"M" |
| 14 | שכבת קו ציר |
| 15 | קו ציר על Rectangle: "0"/"1" |
| 16 | קו ציר על Circle: "0"/"1" |
| 17 | קו ציר על Ellipse: "0"/"1" |
| 18 | שכבת קו חיצוני Geometry (Border + Hole) |
| 19 | שכבת האצ' Geometry |
| 20 | Pattern Geometry |
| 21 | Scale Geometry |
| 22 | Angle Geometry |
| 23 | שכבת קו פנימי Geometry (Hole בלבד) |

---

## משימה פתוחה — עיצוב מחדש של חלון ההגדרות (DCOLSET)

**עדיין לא בוצע. זו המשימה הבאה.**

### מה שאושר עד כה:

1. **סטייל ויזואלי** — כמו חלונות ה-DCL החדשים (כפתורים מסודרים, כותרות מודגשות, עברית).

2. **הסרת תיבות החיפוש** מעל כל רשימת שכבות. הוחלט (אופציה 2): **למחוק לחלוטין** את תיבות החיפוש, ולא להשתמש בדילוג-לאות כתחליף יחיד.

3. **הוספת כפתור Pick לכל שדה שכבה** (8 שדות):
   - שכבת קו מקור, שכבת האצ' מקור, שכבת קו עותק, שכבת האצ' עותק, שכבת קו ציר, שכבת קו חיצוני Geometry, שכבת האצ' Geometry, שכבת קו פנימי Geometry.
   - **התנהגות:** לחיצה על Pick → החלון נסגר זמנית → המשתמש בוחר אובייקט בשרטוט (entsel) → התוסף קורא את השכבה שלו → החלון נפתח מחדש עם השכבה הזו ממולאת ברשימה.
   - זהה לדפוס הקיים כבר בכפתור "שכבה חדשה..." (`newlay`) ב-`col:dlg` — סגירה עם `done_dialog 2`, ביצוע פעולה בשרטוט, פתיחה מחדש.
   - **דורש שינוי מבני ב-`col:dlg`:** יש לשמור את הערכים הנוכחיים (כולל אלה שנבחרו ע"י Pick) במשתנה שמתעדכן בין סגירה לפתיחה מחדש — לא רק `cur` המקורי שהתקבל בכניסה לפונקציה.

4. **הרשימה הנפתחת (popup_list) נשארת** — רק תיבת החיפוש מעליה נמחקת.

### לא הוכרע עדיין / לבדוק בהמשך:
- מבנה מדויק של הקוד ל-8 כפתורי Pick (מיפוי key→אינדקס ב-cur, פונקציית עזר גנרית לקריאת שכבה מאובייקט).
- בדיקה בפועל ב-ZCAD לאחר הכתיבה.

---

## מצב הקוד — מה עובד ואושר

### COL/WALL:
- טעינה אוטומטית
- כל התפריטים בחלונות DCL (ראה מעלה)
- ציור כל הצורות: Polygon / Rectangle / Circle / Ellipse / Multi
- תצוגה חיה: Circle / Ellipse
- Attach — שיוך האצ' חיצוני (בדיקה בפועל עדיין פתוחה)
- קווי ציר — נוצרים, מתויגים, מתעדכנים
- DCOLUP — סנכרון מלא
- Multi — תיקון Escape/חזרה לשלב הקודם (ראה מעלה)

### Geometry:
- **Border**: Rectangle / Polygon / Polyline (פתוח) — קו מתאר + עותק, לעולם בלי חור
- **Hole**: Rectangle / Polygon — תמיד עם חישוב פנימי (0.15) + האצ' + עותק
- שכבות נפרדות: קו חיצוני / קו פנימי / האצ'
- סנכרון ב-DCOLUP

---

## מבנה ה-XData
- אפליקציה: `COL_PAIR` (קבוע)
- 3 שדות: גרסה ("1") + pair-id + role
- pair-id: millisecs + מונה גלובלי

### Roles — COL/WALL רגיל:
| Role | תיאור |
|------|--------|
| `SL` | קו מקור |
| `SH` | האצ' מקור |
| `CL` | קו עותק |
| `CH` | האצ' עותק |
| `SAX` | קו ציר מקור |
| `CAX` | קו ציר עותק |

### Roles — Multi:
| Role | תיאור |
|------|--------|
| `SOL` | קו חיצוני מקור |
| `SIL` | קו פנימי מקור |
| `SH` | האצ' מקור |
| `COL` | קו חיצוני עותק |
| `CIL` | קו פנימי עותק |
| `CH` | האצ' עותק |

### Roles — Hole (Geometry):
| Role | תיאור |
|------|--------|
| `NOL` | קו חיצוני מקור |
| `NIL` | קו פנימי מקור |
| `NH` | האצ' מקור |
| `CNOL` | קו חיצוני עותק |
| `CNIL` | קו פנימי עותק |
| `CNH` | האצ' עותק |

### Roles — Border (Geometry):
| Role | תיאור |
|------|--------|
| `BOL` | קו מתאר מקור |
| `CBOL` | קו מתאר עותק |

---

## DCOLUP — לוגיקה
1. סורק כל האובייקטים עם XData של COL_PAIR
2. מקבץ לפי pair-id
3. לכל זוג: מוחק עותק ישן, בונה חדש לפי המקור + הזחה
4. תומך: COL/WALL רגיל / Multi / Attach / Hole / Border

### טיפול ב-LWPOLYLINE ב-DCOLUP:
הפונקציה
`col:clone-line-offset`
מזהה אם הפולי-ליין סגור או פתוח ויוצר את העותק בהתאם.

---

## פרטים טכניים — ZCAD ספציפי
- יצירת האצ': `-HATCH _LA [layer] _S [ss] "" ""`
- איתור האצ' החדש: `col:find-new-hatch` (entlast לא אמין ב-ZCAD)
- XData: `COL_PAIR` — לא מתנגש עם תוספים אחרים
- תאימות לאחור: `col:ensure-settings` משלים שדות חסרים עד 24 שדות, גם אם הקובץ ישן
- **Escape לא מפעיל באופן אמין כפתורי Cancel בחלונות DCL — חובה כפתור גלוי לכל פעולת ביטול/חזרה**

---

## util:filter-list — עדיין בקובץ, פחות רלוונטי כרגע
```lisp
(defun util:filter-list (lst term)
  (if (>= (strlen term) 3)
    (vl-remove-if-not
      '(lambda (x) (vl-string-search (strcase term) (strcase x)))
      lst)
    lst))
```
נמצאת כרגע בתוך DCOL.lsp. לא בשימוש בחלון ההגדרות אחרי ההחלטה להסיר תיבות חיפוש (אופציה 2). אם בעתיד יוחלט אחרת — אפשר להחזיר.
בעתיד — תועבר לקובץ משותף `Acad\acad_utils.lsp` לשימוש בכל התוספים, אם תידרש במקום אחר.

---

## משימות פתוחות
1. **עיצוב מחדש של DCOLSET** עם כפתורי Pick (ראה פירוט מעלה) — **המשימה הבאה**
2. **בדיקת Attach בפועל** ב-ZCAD — עדיין עובד רק בחלק מהמקרים
3. **בדיקת Geometry בפועל** ב-ZCAD לאחר השינויים (Border עם Polygon, Hole בלי NoInner)
4. **קובץ משותף** `acad_utils.lsp` — כשיהיו מספיק פונקציות משותפות לתוספים
